package sel2;

import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.stream.Collector;
import java.util.stream.Collectors;

public class Summa {
	public static void main(String[] args) {
		int[] a1 = { 5, 9, 6 };
		Arrays.sort(a1);
		System.out.println(a1[1]);
		/*
		 * String s = "sreevathsan"; char[] c = s.toCharArray(); char[] v = { 'a', 'e',
		 * 'i', 'o', 'u' }; ArrayList<Character> a = new ArrayList<Character>(); for
		 * (int i = 0; i < v.length; i++) { int k = 0; for (int j = 0; j < c.length;
		 * j++) { if (v[i] == c[j]) { k = k + 1; a.add(v[i]); } }
		 * System.out.println(v[i] + " is repeated " + k + " times"); }
		 * System.out.println("the vowel stored in new array is " + a +
		 * "and count of vowels is " + a.size());
		 */
	}
}